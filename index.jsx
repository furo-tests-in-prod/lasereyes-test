import React from 'react';
import { createRoot } from 'react-dom/client';
import App from './App';

// Use this in a browser environment:
// const container = document.getElementById('root');
// const root = createRoot(container);
// root.render(<App />);

// For server-side testing only:
console.log('To use this React application:');
console.log('1. Set up a React build environment with webpack or vite');
console.log('2. Install the Unisat wallet browser extension');
console.log('3. Use the LaserEyes SDK to interact with Bitcoin wallets');
console.log('4. The WalletConnector component shows how to use the useLaserEyes hook');