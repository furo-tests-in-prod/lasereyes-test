export declare const createRuneMintScript: ({ runeId, }: {
    runeId: string;
    pointer?: number;
}) => Buffer;
export declare const createRuneEtchScript: ({ pointer, runeName, symbol, divisibility, perMintAmount, premine, cap, turbo, }: {
    pointer?: number;
    runeName: string;
    symbol: string;
    divisibility?: number;
    perMintAmount: number;
    cap?: number;
    premine?: number;
    turbo?: boolean;
}) => Buffer;
export declare const createRuneSendScript: ({ runeId, amount, divisibility, sendOutputIndex, pointer, }: {
    runeId: string;
    amount: number;
    divisibility?: number;
    sendOutputIndex?: number;
    pointer: number;
}) => Buffer;
//# sourceMappingURL=scripts.d.ts.map